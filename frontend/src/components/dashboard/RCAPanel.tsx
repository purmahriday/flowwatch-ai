// Root cause analysis assistant panel.
// Lets the user select an anomaly event and request an LLM explanation
// via POST /assistant/analyze. Renders the structured RCA response with
// probable causes and suggested remediation steps.
